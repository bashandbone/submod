interface:
  display_name: "Submod"
  short_description: "Repo-specific patterns and workflows for submod"
  default_prompt: "Use the submod repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true